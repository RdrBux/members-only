<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css" />
    <title><%= title %></title>
  </head>
  <body>
    <nav class="navbar bg-primary-subtle">
      <div class="container">
        <a class="navbar-brand fw-medium fs-3 text-primary-emphasis" href="/"
          >Members Only</a
        >
        <div>
          <span class="navbar-text"
            >Welcome back, <b><%= currentUser.fullname %></b>.</span
          >
          <a href="/log-out">LOG OUT</a>
        </div>
      </div>
    </nav>
    <div class="container">
      <h1 class="text-center mt-4"><%= title %></h1>

      <div class="row justify-content-center mt-4">
        <form class="col-md-5 col-md-offset-5" method="post">
          <div class="form-group">
            <label for="title">Title</label>
            <input
              class="form-control <%= locals.errors?.some(error => error.path === 'title') ? 'is-invalid' : '' %>"
              type="text"
              name="title"
              id="title"
              required
            />
            <div class="invalid-feedback">
              <%= locals.errors?.find(error => error.path === 'title')?.msg %>
            </div>
          </div>
          <div class="form-group mt-2">
            <label for="content">Message</label>
            <textarea
              class="form-control <%= locals.errors?.some(error => error.path === 'content') ? 'is-invalid' : '' %>"
              style="height: 10rem"
              name="content"
              id="content"
              required
            ></textarea>
            <div class="invalid-feedback">
              <%= locals.errors?.find(error => error.path === 'content')?.msg %>
            </div>
          </div>
          <button class="btn btn-primary mt-4">Add message</button>
        </form>
      </div>
    </div>
  </body>
</html>
