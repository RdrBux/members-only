<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css" />
    <title><%= title %></title>
  </head>
  <body>
    <nav class="navbar bg-primary-subtle">
      <div class="container">
        <a class="navbar-brand fw-medium fs-3 text-primary-emphasis" href="/"
          >Members Only</a
        >
        <div>
          <span class="navbar-text"
            >Welcome back, <b><%= currentUser.fullname %></b>.</span
          >
          <a href="/log-out">LOG OUT</a>
        </div>
      </div>
    </nav>
    <div class="container">
      <h1 class="text-center mt-4"><%= title %></h1>

      <div class="row justify-content-center mt-4">
        <div class="col-md-5 col-md-offset-5">
          <p class="text-center">
            Membership status: <% if (currentUser.member) { %>
            <b class="text-success">member</b><% } else { %>
            <b class="text-danger">not a member</b><% } %>.
          </p>

          <div>
            <% if (currentUser.member) { %>
            <p>You are already a member. Go back to <a href="/">home.</a></p>
            <hr />
            <% } %>
            <form method="post">
              <label for="key">Insert the supersecret key:</label>
              <input
                class="mt-2 form-control <%= locals.errors?.some(error => error.path === 'key') ? 'is-invalid' : '' %>"
                type="text"
                name="key"
                id="key"
                placeholder="Meow! ???"
                required
              />
              <div class="invalid-feedback">
                <%= locals.errors?.find(error => error.path === 'key')?.msg %>
              </div>
              <button class="btn btn-primary mt-2">Send</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
